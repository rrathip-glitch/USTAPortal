---
description: Run a full USTA sync (fetch tournaments, draws, players, WTN, matches) using the CLI.
---

Run the full sync pipeline.

```bash
python -m src.cli.main sync
```

If sync fails, follow the troubleshooting tree in RUNBOOK.md.
